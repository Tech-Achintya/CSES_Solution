#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'

void print(const vector<int>& arr) {
    for (int i = 0; i < arr.size(); i++) {
        cout << arr[i] << " ";
    }
    cout << endl;
}

ll funct(int i,vector<ll> wts){
    if(i==n){
        return 0;
    }
    
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n,cap;
    cin>>n>>cap;
    vector<ll> allWts(n);
    for(int i=0;i<n;i++){
        cin>>allWts[i];
    }

}